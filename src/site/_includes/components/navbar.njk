{%if settings.dgHomeLink === true%}
    <nav class="navbar">
        <div class="navbar-inner">
            <a href="/" style="text-decoration: none;">
                <h1 style="margin: 15px !important;">{{meta.siteName}}</h1>
            </a>
        </div>
        <div class="navbar-inner">
            {%- if lang === "pt" -%}
            <h1 style="background-color: #fafea9; margin: 15px !important;">🇧🇷</h1>
            <a href="https://drops.tao.tec.br/pubnotes/en/home" style="text-decoration: none;">
                <h1 style="margin: 15px !important;">🇬🇧</h1>
            </a>
            {%- endif -%}
            {%- if lang === "en" -%}
            <h1 style="background-color: #fafea9; margin: 15px !important;">🇬🇧</h1>
            <a href="https://drops.tao.tec.br" style="text-decoration: none;">
                <h1 style="margin: 15px !important;">🇧🇷</h1>
            </a>
            {%- endif -%}
        </div>

        {% if settings.dgEnableSearch === true%}
            {% include "components/searchButton.njk" %}
        {%endif%}
    </nav>
    {%else%}
    <div class="empty-navbar" >
        {% if settings.dgEnableSearch === true%}
            {% include "components/searchButton.njk" %}
        {%endif%}
    </div>
{%endif%}